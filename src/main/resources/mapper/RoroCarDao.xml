<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.roroterminal.lksys.dao.RoroCarDAO">
    <select id="getCarList" resultType="CarVO">
        SELECT CAR_ID,
                CAR_NAME,
                FBX_INFO
        FROM    CAR_INFO
    </select>
</mapper>